
#include <stdio.h>
#include <math.h>
#include <time.h>
int main()
{
    
    int n = 20;
    int a = 0;
    int mass[n];
    for (int i = 0; i < n; i++){
        mass[i] = rand()* time(NULL) % 9;
    }
    int new[n]
    int new[0] = int mass[0];
    for (int j = 0; j < n - 1; j++){
        for (int k = 1; k < n; k ++){
            if (new[j] == mass[k]){
                a++;
            }
           else{
                new[j+1]= mass[k];
            }
        }
    }



    
    
    
    for (int l = 0; l < n; l++){
        printf("%d\n", new[l]);
    }
}
